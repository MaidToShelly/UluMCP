# ── WAD Token ──
WAD_CONTRACT_ID=47138068
WAD_DECIMALS=6
WAD_NETWORK=voi-mainnet

# ── Treasury ──
TREASURY_ADDRESS=IR7EOSEN5S3L2HWHW6OXQW6CBG2I2N73VEGI3NXZLYPKZXAP3EUXG4EINU
TREASURY_MNEMONIC=word1 word2 ... word25

# ── Billing Thresholds ──
MIN_REQUIRED_BALANCE=10
MIN_REQUIRED_ALLOWANCE=10
SETTLEMENT_THRESHOLD=1
MAX_UNPAID_USAGE=2

# ── Worker ──
WORKER_INTERVAL_MS=30000
MAX_SETTLEMENT_RETRIES=5

# ── Auth ──
NONCE_EXPIRY_MS=300000

# ── Server ──
MCP_PORT=3000
BILLING_DB_PATH=./billing.db

# ── Tool Pricing Overrides (JSON) ──
# TOOL_PRICES={"arc200_balance_of":"0.002","snowball_quote":"0.01"}

# ── Network Endpoints (optional overrides) ──
# VOI_MAINNET_ALGOD_URL=https://mainnet-api.voi.nodely.dev
# VOI_MAINNET_MIMIR_URL=https://voi-mainnet-mimirapi.nftnavigator.xyz
# HUMBLE_API_URL=https://humble-api.voi.nautilus.sh
